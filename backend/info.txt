# entre na pasta back end: cd backend 

# depois entre na pasta app: cd app

# antes de tudo, baixe todas as dependencias necessárias: pip install -r requirements.txt

# para rodar a api primeiro ative o venv: venv/Scripts/activate

# comando para rodar: uvicorn app.main:app --reload

# para fazer testes da api rodando, acesse a porta docs

http://127.0.0.1:8000/docs
